// Communication Agent: Handles email and notifications
class CommunicationAgent {
  sendNotification(notification) {
    return `Sending notification: ${notification}`;
  }
}

module.exports = CommunicationAgent;
