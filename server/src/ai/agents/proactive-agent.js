// Proactive Agent: Provides proactive recommendations
class ProactiveAgent {
  provideRecommendation(context) {
    return `Providing recommendation for: ${context}`;
  }
}

module.exports = ProactiveAgent;
