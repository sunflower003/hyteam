// AI Interaction Model: Logs AI interactions
class AIInteraction {
  constructor(user, query, response) {
    this.user = user;
    this.query = query;
    this.response = response;
  }
}

module.exports = AIInteraction;
